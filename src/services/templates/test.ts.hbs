import assert from 'assert';

describe('"./src/services/{{pluralize item}}/{{item}}.ts"', () => {
  it('registered the service', async function () {
    const service = this.app.service('{{path}}');

    assert.ok(service, 'Registered the service');
  });
});
